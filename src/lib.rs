//!
//! creates.io 示例
//!
//! creates.io is a collection of utilities for Rust.
//!
//! It provides a set of common functions that are useful in many Rust projects.

/// Adds two numbers together by cbt
///
/// # Example1
///
/// ```
/// assert_eq!(rust_base::add(1, 2), 3);
/// assert_eq!(rust_base::add(3, 4), 7);
/// ```
pub fn add(a: i32, b: i32) -> i32 {
    a + b
}
